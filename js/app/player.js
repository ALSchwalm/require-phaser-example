
define(function(){
    function Player(){
        // Player constructor
    };

    // Create an instance of player
    var player = new Player();

    // Expose the instance (but not the class)
    return player;
})
